# Сборка. Воспроизводится командой `node tools/bundle.js`, поэтому в историю не идёт:
# файл содержит все ассеты в base64 и переписывался бы целиком на каждый коммит.
dist/

# Папка для упаковки в APK. Воспроизводится командой `node tools/make-webdir.js`.
www/

# Мусор окружения
.DS_Store
node_modules/
__pycache__/

# Временная распаковка поставки v11 и её архив: тот же код, что уже в репозитории,
# плюс 67 МБ git-бандла и 94 МБ однофайловой сборки. Держим только на диске.
_tmp_v11/
*.zip

# Служебные файлы сессии Claude Code. launch.json нужен, остальное — мусор.
.claude/share_chat.html
.claude/tmp_pages_index.html
